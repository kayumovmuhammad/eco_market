// project import
import { isAuth } from '../api/auth';
import pages from './page';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [pages]
};

export default menuItems;
